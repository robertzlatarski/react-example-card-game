import styled from 'styled-components';

const styledHeading = styled.div;

const GameHeading = styledHeading`
  font-size: 4rem;
  text-align: center;
  font-weight: bold;
`;

export default GameHeading;
